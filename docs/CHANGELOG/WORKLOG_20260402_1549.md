# Work Log
> 날짜: 2026-04-02 15:49
> 기준 커밋: 816a4c5 PLAN 문서 작성

## Launch 파일

- **[신규]** `woosh_bringup/launch/woosh_navigation_system.launch` — 모든 모드/설정 args를 노출하는 단일 정식 진입점, `mobile_move_server` 노드에 전체 param 전달
- **[수정]** `woosh_navigation_mb/launch/move_base_only.launch` — `global/local_planner_plugin`, 8개 `*_params_file`, `load_*_params` args 추가; 하드코딩 YAML 경로 → arg 참조로 교체
- **[수정]** `woosh_costmap/launch/global_costmap.launch` — `costmap_common_params_file` / `global_costmap_params_file` args 추가; 하드코딩 경로 교체
- **[수정]** `woosh_navigation_mb/launch/navigation.launch` — 파일 상단에 DEPRECATED 헤더 + 대체 명령 주석 추가 (기능 변경 없음)

## Python 스크립트

- **[수정]** `woosh_bringup/scripts/woosh_service_driver.py`
  - `main()`: ROS param 우선 읽기 → 레거시 CLI 플래그 번역 레이어로 재구성 (param-first)
  - `_validate_modes()` 신규: SLAM+loc 동시 실행 차단, nav 단독 실행 차단, `carto_nonfix+nav` 시 map/state_file 동시 검증
  - `StackLauncher.start_base_laser_tf()` 신규: 기본 모드에서 `base_link→laser` 정적 TF 발행
  - `StackLauncher.start_move_base()` 확장: 플래너 플러그인 + YAML 경로 인자 수용 및 `move_base_only.launch`에 전달
  - `wait_for_nav_prerequisites()` 확장: `slam_mode` 파라미터 추가, SLAM 모드에서 `/map` + `TF map→odom` 대기
  - SLAM + navigation 조합(`slam_mode + navigation_mode`) 정식 지원 (기존 차단 로직 제거)

## 설정 / 빌드

- **[수정]** `woosh_navigation_mb/CMakeLists.txt` — `launch/` + `config/` install 타겟 추가

## 문서

- **[수정]** `README.md` — 터미널 3에 "정식 실행(권장)" 섹션 + 인자 표 추가; 기존 rosrun 방식을 "레거시 CLI" 서브섹션으로 정리; 로드맵 섹션 8에 리팩토링 완료 항목 체크
